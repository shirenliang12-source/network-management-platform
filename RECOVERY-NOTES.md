# 1.9.52.1 增强源码候选版

以本地 1.9.51 源码包及生成 1.9.52 前的 16 次补丁恢复 1.9.52，再单独移植设备与关联修复。不是 1.9.56 改名，不引入 PG 自动安装/迁移。

新增：ASA/FTD 的独立会话处理；IOS 路由器、自主 AP、WLC 类型与能力说明；Fortinet/PAN-OS 命令及 LLDP 解析；服务器 IP 表头与设备/IP/机柜关联；管理员 PEM 证书导入与独立 TLS 配置。

CUCM、IP 电话、轻量 AP 仅登记并明确能力限制，未实现完整 CUCM DRS 或电话备份。WLC 文本配置不等于完整恢复包。设备型号、VDOM、固件和账号权限仍需实机验证。

证书首项必须是服务器叶证书，支持 PEM 私钥及可选私钥密码。校验匹配、有效期、用途及 OpenSSL 加载后原子切换配置，旧证书保留。私钥受文件权限保护，不返回页面或记录审计正文。首次导入需本机回环 HTTP 或已有 HTTPS。重启服务后使用 HTTPS，浏览器信任及 SAN 域名/IP 需管理员自行核对。证书存放在数据目录的 tls 子目录，请随数据备份。

## 升级验证版

安装流程已取消按进程名结束所有 NSSM，改用原生服务状态检查；超时中止覆盖。保留旧服务身份、参数和数据目录，健康检查支持 HTTP/HTTPS 并校验版本与证书指纹。已编译 Windows 安装包，但目标旧服务器的服务安装/回滚尚未实机验收，因此仅作为升级验证版发布。详情见 docs/release-1.9.52.1.md。

增强版验证：117 项 Python 回归测试、12 项前端测试通过；证书解析、有效期、OpenSSL 加载和原子保存使用真实测试证书，Windows ACL 外部命令在单元测试中模拟，未宣称实机通过。

恢复版测试通过不代表本次新增功能已经实机验收。独立分支用于代码审查与继续测试，不覆盖 GitHub 主分支；不上传数据库、密钥、运行配置或凭据。
